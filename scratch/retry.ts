import "dotenv/config"; import { Queue } from "bullmq"; import { getRedisConnectionOptions } from "../src/lib/redis"; async function retryJobs() { const q = new Queue("ai-visibility-scrape", { connection: getRedisConnectionOptions() }); const delayed = await q.getDelayed(); for (const job of delayed) { await job.promote(); console.log(`Promoted delayed job ${job.id}`); } const failed = await q.getFailed(); for (const job of failed) { await job.retry(); console.log(`Retried failed job ${job.id}`); } await q.close(); console.log(`Done promoting/retrying. Promoted ${delayed.length}, Retried ${failed.length}`); } retryJobs();
