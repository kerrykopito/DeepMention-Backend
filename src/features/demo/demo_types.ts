export type DemoInput = {
    name: string,
    email: string,
    company: string,
    notes?: string,
    scheduledAt: Date,
    timezone: string,
    countryCode?: string,
    countryName?: string,
    localTimeLabel?: string,
    istTimeLabel?: string,
}
